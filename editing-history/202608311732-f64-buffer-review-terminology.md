# 2026-08-31 17:32 — F64Buffer review terminology

## 中文

- 将 builder 的 “v1 typed-buffer syntax” 改为 “initial typed-buffer syntax”，避免与 `calcit-calx-kernel/1` ABI edition 混淆。
- 修正 RFC 0004 validator 清单，将三条新增指令误写成“两条”的计数改正。

## English

- Reworded the builder diagnostic from “v1 typed-buffer syntax” to “initial typed-buffer syntax” so it cannot be confused with the `calcit-calx-kernel/1` ABI edition.
- Corrected the RFC 0004 validator checklist from two new instructions to the actual three.
