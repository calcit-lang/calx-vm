# Mark calx_vm 0.6.0 published / 标记 calx_vm 0.6.0 已发布

## English

- Record the 0.6.0 GitHub Release, exact-main tag SHA, and publish workflow in `docs/releases/0.6.0.md`, and switch its status from release candidate to published.
- Update README availability wording and the roadmap published baseline to 0.6.0.
- Note that Calcit adopts the exact `calx_vm = "=0.6.0"` pin instead of a path override.

## 中文

- 在 `docs/releases/0.6.0.md` 记录 0.6.0 GitHub Release、精确 main tag SHA 与 publish workflow，并把状态从 release candidate 改为 published。
- 更新 README 的可用性表述与 roadmap 的已发布基线为 0.6.0。
- 说明 Calcit 通过精确 `calx_vm = "=0.6.0"` 固定采用，而不是 path override。
