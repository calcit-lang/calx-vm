# Prepare 0.6.0 Tag/Str release / 准备 0.6.0 Tag/Str 发布

## English

- Bump the crate minor version to 0.6.0 for the strict `Tag`/`Str` identity split merged in #77 with the #76 follow-up.
- Mark `docs/releases/0.6.0.md` as the release candidate and record the exact release order from candidate verification to Calcit's exact dependency pin.
- Update the README published-baseline and install wording to 0.6.0 while keeping availability explicit until the GitHub Release, publish workflow, and crates.io package are verified.
- No parser ownership, opcode, coercion, collection, Nil, Dynamic, or runtime-mode change is introduced.

## 中文

- 为 #77 及 #76 后续合并的 strict `Tag`/`Str` identity 拆分，将 crate minor 版本提升到 0.6.0。
- 把 `docs/releases/0.6.0.md` 标记为 release candidate，并记录从候选验证到 Calcit 精确依赖固定的发布顺序。
- 将 README 的已发布基线与安装版本更新为 0.6.0，但在 GitHub Release、publish workflow 与 crates.io 包完成验证前保持候选表述。
- 不引入 parser ownership、opcode、coercion、collection、Nil、Dynamic 或 runtime mode 变化。
