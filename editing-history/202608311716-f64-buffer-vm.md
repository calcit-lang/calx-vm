# 2026-08-31 17:16 — F64Buffer VM vertical slice

## 中文

- 按 RFC 0004 增加独立 concrete `F64Buffer`，使用 `Rc<[f64]>` 共享不可变 backing；显示和调试只暴露长度。
- 增加显式 share/adopt/copy 构造、严格函数/local/control/import 边界，以及 `len`、checked index conversion、checked `get` 三条指令。
- F64Buffer global、constant literal、truthiness、List/Nil/Dynamic 替代均明确拒绝；host 边界按实际 variant 再检查。
- 增加 parser、builder、validator、lowering、runtime、CLI golden、demo 与 malformed public IR 回归。

## English

- Added a distinct concrete `F64Buffer` backed by shared immutable `Rc<[f64]>` storage; display and debug output expose length only.
- Added explicit share/adopt/copy construction, strict function/local/control/import boundaries, plus `len`, checked index conversion, and checked `get` instructions.
- Explicitly reject F64Buffer globals, constant literals, truthiness, and List/Nil/Dynamic substitutes; host boundaries recheck actual variants.
- Added parser, builder, validator, lowering, runtime, CLI golden, demo, and malformed-public-IR regressions.
