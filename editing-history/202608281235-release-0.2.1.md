# 发布 0.2.1 / Release 0.2.1

## 中文

- 发布 main 上已经合并的 parser ownership 兼容修复、VM allocation/lookup 优化与 benchmark 工作。
- 验证 Rust tests 和 publish workflow 的全部九个 demos。
- 记录严格 Clippy 暴露的 `result_large_err` 设计债务，后续单独处理。

## English

- Release the parser-ownership compatibility fixes, VM allocation/lookup improvements, and benchmark work already merged on main.
- Verify Rust tests and all nine demos from the publish workflow.
- Record the `result_large_err` design debt exposed by strict Clippy for separate follow-up.
